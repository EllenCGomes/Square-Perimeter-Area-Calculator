# receive the square side value
side = input("Enter the corresponding value for the square side:")

# calculate perimeter
perimeter = (int(side))**2

# calculate area
area = (int(side))*4

print(f"perimeter: {perimeter} - area: {area}")
